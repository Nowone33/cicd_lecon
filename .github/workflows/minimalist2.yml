name: minimalistV2

on:
  push:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - run : echo "Je construis"

  test:
    runs-on: ubuntu-24.04
      - run: echo "Je teste"

  deploy:
    runs-on: ubuntu-24.04
    needs:  [build, test]
    steps: 
      - run: echo "Deploy"
